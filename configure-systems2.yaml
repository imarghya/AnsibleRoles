# Playbook to call multiple roles to configure systems
- name: Configure systems
  hosts: 
  become: yes

  tasks:

- name: Call Roles
  hosts: 
  roles:
   #- hostname
   #- firewalld
   #- ntp-config
   - motd
